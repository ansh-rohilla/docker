apiVersion: v1
kind: ReplicationController
metadata:
  name: nginx-rc
  labels:
    app: nginx
    env: demo
spec:
  replicas: 3   
  selector:
    app: nginx
  template:
    metadata:
      labels:
        env: demo
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx  
